from setuptools import setup, find_packages

setup(
    name='party_downloader',
    version='0.0.1',
    url='',
    author='',
    author_email='',
    description='Downloader for the .party sties',
    packages=find_packages(),
    install_requires=[],
    include_package_data=True,
    # scripts=["scripts/compile_report.py"]
)
